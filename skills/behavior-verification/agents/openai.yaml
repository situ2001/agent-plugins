interface:
  display_name: "Behavior Verification"
  short_description: "Verify logic, React interactions, and browser flows"
  default_prompt: "Use $behavior-verification to verify a concrete behavior in the actual source with the smallest faithful runtime, using a function, React component, or browser check as appropriate."
